# 图片讲故事应用 · 技术设计文档

> ISOM5240 深度学习的商业应用 · Individual Assignment
> 学生：Jingxiang MA

---

## 1. 项目概述

一个基于 **Hugging Face Transformers pipeline** 的图片讲故事 Web 应用：用户上传一张图片，应用用深度学习模型从图中提取细节、生成一段 **50–100 词的英文叙事**，并通过文本转语音（TTS）朗读出来。最终部署在 **Streamlit Cloud** 上供在线交互。

---

## 2. 作业要求（原文）与拆解

| # | 原文要求 | 拆解 |
|---|---------|------|
| 1 | 用 Hugging Face **Transformers pipeline** 开发 | 主路径必须本地加载模型、走 `pipeline()`，不能用远程 API |
| 2 | 用户上传图片 | `st.file_uploader` |
| 3 | 生成 **50–100 词**叙事，基于**从图片提取的细节** | 两步：`image-to-text` 提取细节 → `text-generation` 生成叙事 |
| 4 | 故事转成音频 | edge-tts → MP3 → `st.audio` 播放 |
| 5 | 部署到 **Streamlit Cloud** | GitHub 仓库 + Community Cloud |

---

## 3. 技术栈

| 模块 | 方案 | 说明 |
|------|------|------|
| UI 框架 | Streamlit | 部署目标就是 Streamlit Cloud |
| 读图（提取细节） | `pipeline("image-to-text")` | `microsoft/Florence-2-base`（~0.5GB） |
| 故事生成 | `pipeline("text-generation")` | `distilgpt2`（~330MB） |
| 文本转语音 | edge-tts | 微软 Edge 在线 TTS，免费无 key，英文音色 |
| 音频播放 | `st.audio` | 喂 MP3 字节流，内存操作不落盘 |
| 图片预处理 | Pillow | 压缩到长边 1024 再送模型 |
| 配置 | Streamlit Secrets | HF Token（仅 LLM 选项需要） |

### 可选「LLM 生成方式」（保留）

主路径之外，保留一个侧边栏「生成方式」切换项：走 **HF Inference API 远程调用 VLM**（如 `Qwen/Qwen2.5-VL-7B-Instruct`）直接「读图 + 生成故事」。需要 HF Token，作为加分展示项。

---

## 4. 架构与流程

```
用户上传图片
      │   Pillow 压缩（长边 ≤ 1024）
      ▼
pipeline("image-to-text")   → 提取图片细节描述（caption）
      │
      ▼
pipeline("text-generation") → 基于细节生成 50–100 词英文故事
      │
      ▼
展示故事文本
      │
      ▼
edge-tts 合成 → MP3 字节流 → st.audio 播放朗读
```

**生成方式分支**（侧边栏切换）：

- `Pipeline`（默认）：上述两步，本地小模型。
- `LLM API`（可选）：远程 HF Inference API 一步完成。

---

## 5. 模型选型

| 环节 | 首选 | 备选 | 理由 |
|------|------|------|------|
| 读图 | `Salesforce/blip-image-captioning-base` | `microsoft/Florence-2-base` | BLIP 是标准 `pipeline("image-to-text")` 用法，无需 `trust_remote_code`、输出干净（实现时确定） |
| 生成 | `distilgpt2`（82M） | `gpt2`（124M） | 最省内存，Cloud 免费版 ~1GB 内存能扛 |

> 说明：GPT-2 系列写故事能力有限，50–100 词英文段落可读即可；课程评分点是「用 pipeline 跑通」，非文学性。高质量输出由「LLM 选项」覆盖。

---

## 6. 性能策略（关键）

- **`@st.cache_resource` 缓存模型**：Streamlit 每次交互会 rerun 脚本，模型必须缓存、只加载一次、常驻内存，否则每次都重载（这是同学「两分钟」的主因之一）。
- **同时加载两个小模型**：加载开销 >> 推理开销，「用一杀一」每次生成都重复加载，更慢。同载合计 ~800MB，压在 1GB 线内。
- **下载走 HF 本地缓存**：首次下载后，同一实例内不再重复下载。
- **预期体验**：首次启动 ~30s–1min（下载+加载，一次性），之后每次「生成+朗读」全程 ~5s 内。

---

## 7. TTS 实现（edge-tts）

- **原理**：edge-tts 是微软 Edge「大声朗读」云服务的非官方客户端，运行在**服务器端**，与用户浏览器无关。
- **流程**：故事文本 → `edge_tts.Communicate(text, voice, rate)` → WebSocket 请求微软 TTS 端点 → 异步收集 MP3 音频 chunk → 拼成完整 MP3（内存 `BytesIO`，不落盘）→ `st.audio(bytes, format="audio/mp3")`。
- **可调参数**：`voice`（音色）、`rate`（语速）。
- **英文音色候选**：Aria / Jenny / Emma（US 女声）、Guy / Andrew（US 男声）、Sonia / Ryan（UK）。

---

## 8. 部署方案（Streamlit Cloud）

1. 准备 HF Token（仅 LLM 选项需要）→ <https://huggingface.co/settings/tokens>。
2. 代码推 GitHub（`app.py` 在仓库根目录）。
3. <https://share.streamlit.io> → New app → 选仓库/分支/入口 `app.py` → Deploy。
4. Settings → Secrets 填 `HF_TOKEN`（如启用 LLM 选项）。

---

## 9. 侧边栏选项（交互设计）

- **生成方式**：`Pipeline`（默认） / `LLM API`（需 HF Token）
- **风格**：Fairy tale / Sci-fi / Mystery / Heartwarming / Adventure / Poem
- **长度**：~50 / ~75 / ~100 词（贴合作业 50–100 区间）
- **音色**：上述英文音色
- **语速**：Slow / Normal / Fast

---

## 10. 界面设计（定稿）

- **风格**：暖纸质感（A 方案）—— 暖米色纸张噪点纹理 + 毛玻璃卡片 + 半透明暖木相框 + 琥珀主色。
- **语言**：全英文（侧边栏、标签、按钮、提示全部英文；TA 虽懂中文但会扣分）。
- **形状**：全部直角矩形（`border-radius: 0`）。
- **阴影/高光**：默认无任何高光阴影；仅按钮 hover 时出现顶部高光。
- **配色**：
  - 底色 `#f2e9d8`（暖米）+ 噪点纹理（CSS 内联 SVG）
  - 主色（按钮）`#c7782e` 暖琥珀橙，hover `#d5893c`
  - 文字 `#3a2e23`（深棕）、次要文字 `#8a7a66`
  - 相框：暖木外框 `rgba(169,116,60,.65)` + 卡纸内衬 `rgba(247,236,216,.65)`（35% 透明）
- **布局与状态**：
  - 初始（未上传）：仅显示 `preview`（空相框 + "Your image will appear here"）、`drag & drop`、`Generate Story` 三样；相框在上、上传区在下。
  - 生成后：`Story` + `Read Aloud` 从相框下方滑出；`drag & drop` + `Generate` 下移垫底。
- **动效**：故事/播放器「淡入 + 下滑 + 高度展开」，缓动 `cubic-bezier(.22,.61,.36,1)`，播放器延迟 0.08s 出场。
- **色弱友好**：分层靠明暗 + 线条 + 文字，不靠红绿、不靠微弱阴影。
- **落地**：以上全部通过 `st.markdown` 注入 CSS 实现（背景纹理、毛玻璃、相框、动效）。

## 11. 图片输入规范

- **格式白名单（与硬限制统一）**：`PNG / JPG / JPEG / BMP / TIFF`（含 `.tif` 与 `.tiff` 两种扩展名）。
  - 这五种均为 Pillow 原生支持，故「白名单放行 = 一定能解码」，白名单与硬限制完全一致。
- **最大文件大小**：12MB。
  - `st.file_uploader` 无直接 size 参数，代码中检查 `uploaded.size` 超限即拦截；
  - 另在 `.streamlit/config.toml` 设 `[server] maxUploadSize = 12` 作服务端兑底。
- **兑底**：`Image.open()` 外包 `try/except`（防改扩展名/损坏文件，报错友好不崩）。
- **不支持**：HEIC/HEIF（iPhone 默认）、WebP、GIF、AVIF 均不在白名单，不接收。

## 12. 设计与实现补充决策（开工前评审）

- ✅ CPU 推理速度预期：纯 CPU 环境，「生成故事」约 10~20s，首次冷启动（下载+加载）约 1~2min。
- ✅ EXIF 方向：`ImageOps.exif_transpose()` 处理手机照片旋转，否则竖拍会横躺。
- ✅ 内存/OOM：caption 模型先加载、两模型均 `@st.cache_resource` 常驻；OOM 时降级更小模型。
- ✅ torch 依赖：requirements 含 torch（CPU 版），Cloud 首次构建较慢。
- ✅ 故事/音频解耦：story 文本缓存，改音色/语速只重跑 TTS，不重跑模型。
- ❌ 加载状态分级提示：不做（用简单 `st.spinner` 即可）。
- ✅ 换图重置：上传新图清空旧 story/audio（session_state）。
- ✅ 播放器样式：注入自定义 HTML `<audio>` + CSS 贴近主题（非原生 `st.audio`）。
- ✅ 边界：未上传点 Generate 拦截提示（全英文）。
- ✅ 故事正文换衬线字体（Georgia），提升故事书质感。
- ✅ 读图模型定为 BLIP（实现时调整）：BLIP 是纯 `pipeline("image-to-text")` 用法、无需 `trust_remote_code`、输出干净；Florence-2 需 `trust_remote_code` + 后处理，pipeline 集成不干净。
- ✅ 加载策略定为「用一杀一」（实现时调整）：Cloud 1GB 内存无法同时容纳两个 ~900MB 模型，故读图/编故事各自按需加载、用完 `del` + `gc.collect()` 释放，内存峰值仅为单个模型。
